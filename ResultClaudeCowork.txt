I'll start by reading the handbook to understand the system, then check the browser testing tools.I have the handbook — a Flutter-based POS system with 15 test cases. Note the handbook describes a local 3-port setup, but the deployed Vercel app is a single URL, so I'll need to discover what's actually testable there. Let me check the browser connection and load the interaction tools.Browser is connected. Let me open the POS site to see what's actually deployed before finalizing the test scope.I'll navigate to the POS site and take a screenshot to see the initial state.The deployed app is live and testable — it shows a login screen with a hint for the Van Staff account (`pos1 / pos123456`). The handbook also references `admin` and `hqmanager` accounts for backoffice flows. Before I run the full test pass, two quick scope questions:Scope set: Van Staff (`pos1`) flows + customer screen, output as Markdown. Let me set up the task list and begin.The viewport resized and my earlier clicks missed. Now I have correct coordinates. Let me fill the login form.Credentials entered. Now logging in and capturing the network/console activity.Login succeeded — it's loading ("checking for pending bills..."). Let me wait and capture the POS screen.POS screen loaded successfully as Van Staff ("POS Cashier 1 / สาขา 1"). I notice the header has only 6 icons, but the handbook expects 10. Let me zoom in to identify each header button precisely and check the network/console state.Pixel-zooming is imprecise. Let me read the accessibility tree to get the actual button labels/tooltips, which is more reliable for identifying each control.Flutter renders to canvas, so the accessibility tree is empty — I'll work visually. Let me hover over each header icon to reveal its tooltip, starting with the leftmost.Tooltips don't render on hover in this Flutter build. I'll identify buttons by clicking them. First, let me capture the network activity from login to understand the backend API.Network tracking is now active. Let me start Case 1 (basic sale): search for a product.A product search modal opened. Let me type the query into its field and search.Search for "Sample" returned no results. Let me check the network request to understand the API and what products exist.Backend API is at `jaiheng-pos.onrender.com`. The "Sample" query returned 200 but empty. Let me query the API directly to discover what products actually exist in the deployed database.Let me query the products API directly within the page (using the stored token without exposing it) to see what products exist.The token is protected from JS access. I'll use the actual UI search instead (which is the real test). Let me clear the field and try a broad search to discover available products.The product list loaded — 45 products (MDF boards, P0001–P0009 visible). The "Sample" term from the handbook doesn't exist here; real products are MDF items. Let me add a product to the cart.The product dialog stays open after adding (allows multiple adds). Let me close it and check the cart state.Major finding: adding a product fails. An error toast appeared:

`เพิ่มสินค้าไม่สำเร็จ: Exception: Failed to add item to bill: 400 {"error":"invalid_address_code","message":"Part must be sold from this POS vehicle store"}`

The cart stays empty. Let me capture the exact failing network request.A bill auto-creates (ID `20260629000003`), but `PUT /bills/{id}/add-item` returns **400 invalid_address_code**. Let me check the console and confirm this is systemic by trying another product.Console tracking is now active. Let me try a different product to confirm the error is systemic and capture the console output this time.Viewport resized again. Let me click the "+ เพิ่ม" for P0005 (MDF 9) at its current position.Let me close the dialog and capture the resulting error state, network, and console.Confirmed systemic — same `invalid_address_code` 400 error for P0005 too. This blocks the entire sale flow. Let me capture the precise network requests and console errors.The network output was huge. Let me filter to just the relevant bill requests to capture details cleanly.Confirmed: the bill auto-creates but every `add-item` returns 400 `invalid_address_code` ("Part must be sold from this POS vehicle store") — a systemic blocker for all sale flows. Let me record progress and move on to test the header buttons, which may work independently.Now let me test the header buttons one at a time. Starting with the red "return" icon (คืนสินค้า, Case 5).The return dialog ("คืนของ - อ้างอิงบิลเดิม") opens correctly. Let me test the bill search inside it.The return bill-search works correctly — it fetched bill `20260629000001` and properly reported `invalid_bill_status` (status 'cancelled', needs 'completed'). So the return dialog functions; only end-to-end return is blocked by lack of a completed bill. Let me close and test the next icon (pause).Return dialog closed. Now let me click the pause (2nd) icon to test its function.The pause icon opens the "Held bills" (บิลที่ถูกพักไว้) list dialog. Let me wait for it to load.Held-bills viewer works (empty list loads correctly). Let me close and test the 3rd icon (warning triangle).The dialog close/open timing is overlapping. Let me close it cleanly first.Clean now. Let me click the 3rd icon (warning triangle).The 3rd (warning) icon opens a "Low stock" (สต๊อกใกล้หมด / below reorder point) dialog — not a discount dialog as the handbook implies. Let me wait for it to load.The low-stock dialog works — and reveals products DO have stock (100 each). This confirms the add-item failure is an address-mapping/config issue, not missing stock. Let me close and test the 4th icon (history/clock).The close/open animation timing keeps overlapping. Let me close and confirm a clean state before the next click.Clean state. Now the 4th icon (history/clock = bill history, Case 15's 🕐).The clock opens "ประวัติบิลวันนี้" (Today's bill history). Let me wait for it to load.Bill history works (empty: "no bills today"). Let me close and test the 5th icon (calculator-grid).Clean. Now the 5th icon (calculator-grid).The 5th icon opens "ปิดยอดประจำวัน" (Daily Close, Case 9). Let me wait for it to load the summary.Daily Close (Case 9) works — it loads the summary correctly (all zeros, no sales today; "ปิดยอด" button disabled as expected). 

Key structural finding so far: the deployed header has only 6 buttons. Stock Count (📦), Receive Transfer (🚚), and Requisition (📋) buttons from the handbook are **absent**. Let me close and test the member feature.The dialog animation overlapped again (note: "ปิดยอด" button is now solid/enabled even with 0 sales — worth noting). Let me close it cleanly first.Clean. Now testing the member feature — clicking "ผูกสมาชิก" (link member).The link-member button did nothing with an empty field. Let me enter a phone number in the member field and search (Enter).The phone shows in the field, but it returned a toast "กรุณากรอกเบอร์โทรสมาชิก" (please enter member phone) — Enter didn't register the value. Let me click the "ผูกสมาชิก" button now that the field has a value.The phone "0812345678" is visibly in the field, yet "ผูกสมาชิก" still returns "please enter member phone number" — suggesting the field value isn't captured by the handler. Let me check the network to see if any member-lookup request fired.A member-lookup request did fire: `PUT /bills/{id}/add-member-by-phone` → **404** (member not found for that phone). So the lookup works for the button path, but the Enter-key path incorrectly shows "please enter phone". Also, there's no new-member registration (👤+) button to create a member to test against.

Let me record progress and test the customer screen, which is central to the request. Opening it in a new tab.Let me open the customer-facing screen in a new tab to test it.The customer screen loads correctly — "ยินดีต้อนรับ" (Welcome), 0 items, idle message, and a read-only footer. But the top-right badge reads "กำลังเชื่อมต่อ" (Connecting…). Let me check whether it connects, and inspect its network/console.The customer screen threw an exception (`main.dart.js` Error) and the "Connecting" badge persists. Let me inspect the sync mechanism (the POS uses `pos_mirror_state` in localStorage) and recheck the badge.The reflection works at the data layer — the POS writes full state to `localStorage.pos_mirror_state` (`billId`, `items`, totals), and the customer screen reads it (showing the matching empty cart). But the badge stays "กำลังเชื่อมต่อ" (Connecting) and there was a console exception. Let me verify whether the customer UI actually re-renders on a state change by simulating a mirror update.This is a strong result. The customer screen **did reflect the change**: behind the dialog it now shows "1 รายการ — MDF 9 มิล (P0005), qty 2, ฿540.00" with subtotal ฿540 and net total ฿540. So the POS→customer mirror rendering works (it reads `pos_mirror_state` and renders items/totals correctly).

Note: the "บิลที่ค้างอยู่" (pending bill) dialog appeared after my synthetic injection, so I'll verify it's not a normal-load issue by reloading cleanly.You've hit your session limit · resets 4:20pm (Asia/Bangkok)